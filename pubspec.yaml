name: visualizeit_slides_extension
description: "VisualizeIT slides extension"
version: 1.0.0
#homepage:
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

environment:
  sdk: '>=3.2.6 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  visualizeit_extensions:
    git: #Read only token for extensions.. Change to public URL when available
      url: https://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@github.com/it-toolkit/visualizeit_extensions.git

  auto_size_text: ^3.0.0
  audioplayers: ^5.2.1
  diff_match_patch: ^0.4.1
  syntax_highlight: ^0.4.0
  video_player: ^2.8.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

   assets:
     - assets/docs/
     - assets/
     - assets/backgrounds/


   fonts:
     - family: Inter
       fonts:
         - asset: packages/visualizeit_slides_extension/fonts/inter.ttf
     - family: JetbrainsMono
       fonts:
         - asset: packages/visualizeit_slides_extension/fonts/jetbrainsmono.ttf

